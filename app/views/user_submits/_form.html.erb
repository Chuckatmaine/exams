<%= form_for(@user_submit) do |f| %>
  <% if @user_submit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_submit.errors.count, "error") %> prohibited this user_submit from being saved:</h2>

      <ul>
      <% @user_submit.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :exam_id %><br />
    <%= f.number_field :exam_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
