<%= form_for(@question_course) do |f| %>
  <% if @question_course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@question_course.errors.count, "error") %> prohibited this question_course from being saved:</h2>

      <ul>
      <% @question_course.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :question_id %><br />
    <%= f.number_field :question_id %>
  </div>
  <div class="field">
    <%= f.label :course_id %><br />
    <%= f.number_field :course_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
