<%= form_for(@test_course) do |f| %>
  <% if @test_course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@test_course.errors.count, "error") %> prohibited this test_course from being saved:</h2>

      <ul>
      <% @test_course.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :test_id %><br />
    <%= f.number_field :test_id %>
  </div>
  <div class="field">
    <%= f.label :course_id %><br />
    <%= f.number_field :course_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
