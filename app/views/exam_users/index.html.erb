<h1>Listing exam_users</h1>

<table>
  <tr>
    <th>Exam</th>
    <th>User</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @exam_users.each do |exam_user| %>
  <tr>
    <td><%= exam_user.exam_id %></td>
    <td><%= exam_user.user_id %></td>
    <td><%= link_to 'Show', exam_user %></td>
    <td><%= link_to 'Edit', edit_exam_user_path(exam_user) %></td>
    <td><%= link_to 'Destroy', exam_user, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Exam user', new_exam_user_path %>
