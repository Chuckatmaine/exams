<%= form_for(@exam_report) do |f| %>
  <% if @exam_report.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@exam_report.errors.count, "error") %> prohibited this exam_report from being saved:</h2>

      <ul>
      <% @exam_report.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :exam %><br />
    <%= f.number_field :exam %>
  </div>
  <div class="field">
    <%= f.label :report %><br />
    <%= f.number_field :report %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
