<%= form_for(@question_level) do |f| %>
  <% if @question_level.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@question_level.errors.count, "error") %> prohibited this question_level from being saved:</h2>

      <ul>
      <% @question_level.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :question_id %><br />
    <%= f.number_field :question_id %>
  </div>
  <div class="field">
    <%= f.label :level_id %><br />
    <%= f.number_field :level_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
