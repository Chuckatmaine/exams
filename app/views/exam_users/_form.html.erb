<%= form_for(@exam_user) do |f| %>
  <% if @exam_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@exam_user.errors.count, "error") %> prohibited this exam_user from being saved:</h2>

      <ul>
      <% @exam_user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :exam_id %><br />
    <%= f.number_field :exam_id%>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
